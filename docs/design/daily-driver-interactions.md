# Daily-driver interaction backlog

## Purpose

Asterlyn should become useful through complete developer workflows before spending its main capacity on release-platform polish. Cross-platform compilation and process-liveness checks remain regression gates, but interactive parity on every operating system does not block the next product slice.

The interaction study uses locally installed Android Studio and the documented Rebased study only as behavioral references. Asterlyn does not extract source, assets, icons, fonts, or branded composition from either installed product; standalone open-source dependencies follow their own reviewed upstream license and provenance.

## 2026-09-08 usability audit

The current Git workbench already supports repository open, tracked-first status, patch inspection, stage/unstage, commit, recent history, and branch presentation. The highest-value gaps observed in the running application are:

1. **Duplicated window chrome:** the native title bar sits above Asterlyn's 44 px top bar, wasting roughly one toolbar row and visually separating window controls from application navigation.
2. **Incomplete commit exploration:** selecting history exposes metadata but not the commit's changed files or patches, forcing the user back to another Git tool for ordinary investigation.
3. **Low-density change interaction:** change groups lack filtering, multi-selection, and explicit diff presentation controls for larger working trees.
4. **Read-only branches:** branch data is visible, but checkout and safe branch creation are absent.
5. **No remote round trip:** fetch, pull, push, and actionable upstream state remain outside the product.
6. **Shallow keyboard flow:** primary controls are reachable, but there is no coherent action search, list navigation model, or shortcut discovery surface.

Android Studio demonstrates useful interaction patterns: one compact top-level bar, persistent tool-window navigation, action-dense but contextual commit controls, searchable history, a branch tree beside the commit graph, and details revealed without replacing the working context. These are workflow observations, not a visual template.

## Ordered delivery slices

### U1 — Compact desktop shell

- Remove redundant native title bars on desktop targets.
- Keep a single draggable 44 px application bar with minimize, maximize/restore, and close controls.
- Preserve keyboard focus, visible hover/pressed states, window resizing, and browser-demo behavior.

**Implemented and locally accepted on 2026-09-08.** The Linux release window removed the native title row while retaining the same 1,320 × 820 content window, recovering approximately 40 px of outer vertical space. Automated pointer checks exercised minimize/restore, maximize/restore, drag, and close against the real Tauri window. The controls expose focus-visible styling and accessible names; browser demo mode omits them. Frontend checks/build, delivery-script tests, the 10-test Rust workspace, Clippy, release compilation, and Debian bundling passed. Windows/macOS appearance and control interaction remain unverified until the next CI and platform review; this does not block U2.

### U2 — Complete commit inspection

- Load the selected commit's changed-file summary without blocking history navigation.
- Show a file list and read-only patch for the selected commit.
- Add history text/hash filtering and useful empty/error/loading states.
- Reject stale commit-detail responses when selection changes quickly.

**Implemented and locally accepted on 2026-09-08.** Commit selection now lazily loads a NUL-delimited changed-file list and then only the selected file patch. Root commits compare with the empty tree; ordinary and merge commits compare with their first parent. The history filter matches message, author, email, decoration, short hash, and full hash. `Ctrl/Cmd+F`, `Escape`, `Enter`, arrow keys, `Home`, and `End` cover filter and list navigation while CodeMirror retains its own patch search. Commit/file request generations, repository root, object ID, and selected path must all still match before a response is presented; loading, empty, truncation, command-failure, and retry states remain visible.

Local acceptance used the real Tauri window against the Asterlyn repository: commit changes loaded into the two-pane file/patch view, filtering reduced and emptied the history list correctly, and keyboard navigation changed both commits and files while preserving the current patch. The 13-test Git core suite covers root, ordinary, rename, deletion, addition, merge-first-parent, invalid object ID/path, and delimiter-safe paths. TypeScript checking, the production frontend build, the desktop Rust check through the local WebKit environment, and Git core tests passed. Current limits remain the 150-commit snapshot window, 4 MiB per-file patch cap, first-parent-only merge inspection, and lossy display of non-UTF-8 paths. These are explicit follow-up constraints rather than blockers for U3.

### U3 — Scalable working-tree review

- Add change filtering and keyboard list navigation.
- Support multi-selection for stage/unstage while preserving partial-stage truth.
- Add unified/split and whitespace presentation controls without changing Git content.

Selection is scope-specific: the staged and working-tree rows for a partially staged path are distinct selectable items. Plain click or keyboard traversal establishes the primary patch, `Ctrl/Cmd` toggles items, and `Shift` selects a visible range. Batch stage acts only on selected working-tree rows; batch unstage acts only on selected index rows. A filter narrows visible rows and group actions but does not silently discard hidden selections, so the toolbar always reports the full selected count. After a mutation, selections migrate to the corresponding side only when that side exists in the fresh Git snapshot.

**Implemented and locally accepted on 2026-09-08.** Changed files can be filtered by path, original path, status, scope, or conflict state; `Ctrl/Cmd+F`, `Escape`, `Enter`, arrow keys, `Home`, `End`, `Space`, `Ctrl/Cmd`, and `Shift` support search and scoped selection. A two-file selection was staged and then unstaged through the real Tauri window, and the fresh snapshots moved both selected rows between working-tree and index scope without touching unrelated paths. Hidden selections remained counted while filtering.

At U3 acceptance, working-tree and commit patches shared `Unified`, `Split`, and `Whitespace` controls. Split mode derived aligned before/after documents from the canonical patch, kept replacement blocks and no-newline markers side-specific, disabled wrapping to preserve row alignment, and synchronized vertical scrolling while allowing independent horizontal inspection. U8 supersedes that last interaction by linking both axes. Whitespace mode adds visible spaces, tabs, and trailing-space emphasis without re-running Git. Four focused projection tests cover replacements, addition/deletion-only blocks, no-newline markers, and hunk content that resembles file headers. The production frontend build, TypeScript check, seven delivery/presentation script tests, 13 Git core tests, desktop Rust check, and strict workspace Clippy passed.

Known limits are explicit: U3 did not yet provide intraline word highlighting, moved-block detection, or source-file line-number gutters; its line numbers described aligned patch rows. U6 supersedes that presentation with hunk-derived source line gutters and conservative intraline highlighting, while moved-block detection and complete-file context remain deferred. Large patches remain bounded at 4 MiB. These limits do not affect Git state and do not block safe local branch work in U4.

### U4 — Safe local branch work

- Add checkout and branch creation with dirty-worktree preflight.
- Explain blocked operations and preserve the current repository when switching is unsafe.
- Keep delete, force, reset, and history rewriting outside this slice.

The initial safety contract is deliberately conservative: U4 switches existing local branches and creates a local branch from the current `HEAD` only when the full working tree is known to be clean. Staged, unstaged, conflicted, and untracked paths all block mutation. The interface explains the blockers first, while the Git core repeats the complete preflight immediately before `git switch`; it accepts only a selected `refs/heads/*` ref or a literal name that passes `git check-ref-format`. Automatic stash, carrying local changes, remote-branch materialization, deletion, force, and history rewriting require separate recovery-aware slices.

**Implemented and locally accepted on 2026-09-08.** The branch inspector now distinguishes the current branch, switchable local branches, remote-only refs, pending or failed untracked discovery, and dirty-worktree blockers before enabling a mutation. The real Tauri window showed all five modified Asterlyn paths and disabled branch creation in the dirty repository. A separate clean clone then created and checked out `feature/u4-native` through the interface, switched back to the existing `main` branch, moved branch selection from the keyboard, and rejected `bad name` without changing `HEAD`. The refreshed snapshot, branch list, status bar, and inspector all followed each successful mutation.

The 15-test Git core suite covers clean checkout and creation, tracked and untracked blockers, invalid and duplicate names, option-shaped local refs, and rejection of remote refs in addition to the earlier status, history, and patch contracts. TypeScript checking, the production frontend build, the desktop Rust check through the local WebKit environment, and strict workspace Clippy passed. Asterlyn does not hold an operating-system lock across preflight and `git switch`; Git remains the final authority if another process changes the repository in that short interval. This slice still provides no automatic stash, dirty-change carry, remote-branch materialization, deletion, force, reset, or history rewriting.

### U5 — Remote daily loop

- Add fetch first, then pull and push with explicit upstream and divergence previews.
- Introduce credential integration without storing secrets in application state or logs.
- Keep force push and provider-specific pull-request workflows deferred.

The initial remote contract is intentionally narrow. Asterlyn accepts a configured remote name but never imports its URL into application state. Fetch updates the standard `refs/heads/*` to `refs/remotes/<remote>/*` mapping without tags, submodules, or pruning. Pull is a cancellable upstream fetch followed by a second branch/upstream/operation/cleanliness check and a fast-forward-only merge to the fetched commit ID. Push uses an explicit non-force refspec to the configured upstream, or publishes a same-named new branch to the selected remote and sets upstream only after success. Mirror remotes, custom fetch namespaces, local-dot upstreams, divergence, force, tag propagation, signing, submodule recursion, and credential prompts are rejected or deferred rather than interpreted silently.

Remote processes receive null stdin, non-interactive credential settings, and no inherited askpass or Git tracing. Configured credential helpers and SSH agents remain trusted system integrations; their secrets are never represented by Asterlyn models. Output is retained only in a small transient buffer for failure classification and is then discarded. Cancellation targets the matching repository operation and performs no rollback: fetch tracking refs or local pull state may already have changed, while a cancelled push has an unknown remote outcome until a later successful fetch.

**Implemented and locally accepted on 2026-09-08.** A compact top-bar Sync popover now exposes the selected remote, exact source and destination refs, last-known ahead/behind state, Fetch, fast-forward Pull, Push or Publish, blocked reasons, credential boundary, and an operation-specific Cancel control from every workspace view. A local bare-remote exercise fetched one new peer commit, changed the visible state to behind by one, fast-forwarded the local branch to the exact remote commit, pushed one ahead commit, and published `feature/native-publish` with `origin/feature/native-publish` configured as upstream. A deliberately blocked remote process showed the cancellable state and its whole process group was gone after cancellation. A failing helper emitted a URL containing synthetic credentials and query data; the interface displayed only the typed unknown-failure guidance and no child output.

The 20-test Git core suite now includes remote metadata, unsupported mappings, dirty-worktree fetch/push, clean fast-forward pull, divergence, ordinary push, first publish, mirror rejection, detached `HEAD`, pre-cancellation, and failure classification. Eleven frontend script tests cover remote selection and Fetch/Pull/Push policy in addition to diff and delivery behavior, while the Tauri registry test checks per-repository serialization and exact cancellation IDs. U5 deliberately has no pruning, transfer-progress meter, interactive credential prompt, force or lease push, tags, submodule recursion, signing, custom fetch namespace, local-dot upstream, merge, or rebase. Cancellation never claims rollback, and a cancelled push remains indeterminate until Fetch succeeds.

### U6 — Persistent IDE workbench

- Replace mutually exclusive whole-page views with a fixed Files, Branches, Changes activity rail.
- Keep one permanent center editor while Files or Changes occupies the left dock and Branches independently occupies the bottom dock.
- Arrange Branches as branch/ref navigation, commit history, and commit/file details in three columns.
- Make the left dock, bottom dock, Branches columns, and side-by-side Diff boundary pointer- and keyboard-resizable with versioned local persistence.
- Route working-tree and commit-file selections into typed editor documents without direct cross-zone DOM calls.
- Present bounded patches as source-like side-by-side rows with real hunk line numbers, omitted-context markers, aligned gaps, and conservative intraline highlighting.

U6 deliberately freezes the long-term layout contract before expanding Git operations. Its Files tree is read-only and includes tracked plus non-ignored untracked paths; file creation, deletion, rename, editing, watching, save/recovery, encodings, and complete-file Diff reads remain Stage 3 capabilities. The activity rail itself is fixed-width because it is a control strip rather than a content pane. Layout references from Android Studio and Rebased remain behavioral studies only; Asterlyn does not extract their source, assets, icons, fonts, branding, or screenshots from either installed distribution.

**Implemented and locally accepted on 2026-09-08.** Compared with the previous three mutually exclusive whole-page modes, the new shell supports both required simultaneous arrangements (`Files + Branches` and `Changes + Branches`) while preserving the active editor document. All five content boundaries are now adjustable, up from zero: left/editor, editor/bottom, branch/log, log/details, and before/after Diff. Pointer dragging, arrow keys, `Home`, `End`, reset, viewport clamping, malformed-persistence fallback, and exclusion of repository/session state from persisted layout were exercised. The bottom Git tool keeps branch refs, commit history, and branch or commit-file details in three columns. At U6 acceptance, selecting a branch changed only its details; U8 supersedes that behavior so the selected ref also scopes the history column. Selecting a working-tree or commit file continues to open the center editor.

The source-like split view removes patch headers and line prefixes, uses real old/new hunk line numbers, aligns additions and deletions with explicit gaps, marks omitted ranges and no-newline records, highlights conservative changed substrings, and reports binary content without pretending it is text. A same-scope refresh check kept the working-tree Diff for `repository.rs` open while unstaging that path's independent index side: the visible changed-file count moved from five to four and the active worktree document was reloaded under a new content revision. A focused regression now proves that equal document identity cannot reuse the loading or previous patch revision key.

Local acceptance passed 20 frontend and delivery tests, 21 Git-core tests, two Tauri tests, strict Clippy, TypeScript checking, and the production frontend build. The release binary remained alive in the automated six-second smoke window. The real Linux release window then completed two `working Diff -> project file -> working Diff` cycles against an isolated repository and exited normally through its window close action. Tauri produced Debian, RPM, and AppImage packages; the final package run produced a 12 MiB binary, 3.4 MiB Debian and RPM packages, and an 87 MiB AppImage.

The comparison conclusion is **improved**: the required concurrent work contexts and every planned content splitter are present, the permanent editor survives unrelated tool changes, and prior U1-U5 Git behavior remains covered. The evidence does not claim lower runtime memory or faster Git operations because U6 changed layout and presentation rather than those performance paths. Remaining limitations are the 5,000 tracked-path UI cap after Git output has already been buffered, patch-derived rather than complete-file Diff content, no moved-code detection, a read-only Files route, and no Windows/macOS interactive verification. The next product action is Stage 3's bounded file-content, editor buffer, save, and recovery contract; future large-repository work should stream or incrementally enumerate project paths before increasing the visible cap.

### U7 — Desktop interaction fidelity

- Present each active activity-rail item as one rounded accent tile, without a second edge marker.
- Treat each Changes row as the complete selection target. Selection uses row background plus the existing `aria-selected` state; checkbox-like decoration and an additional primary stripe are removed while modifier, range, and keyboard selection remain intact.
- Make changed files the first substantive region in commit details and give their list the available height. Author, date, object identity, ancestry, and message remain available afterward in a collapsed semantic disclosure.
- Add a visible Hide action to the Git tool-window header. It closes only the bottom tool through the existing persisted layout transition and does not replace or clear the active editor document.
- Open repositories through the operating system's folder chooser in the desktop runtime. Cancellation leaves the current workspace unchanged, errors stay visible, and the manual absolute-path dialog is retained only as an explicitly labeled browser-demo fallback.
- Maintain one original Asterlyn SVG mark with a measurable transparent safe area, then regenerate the complete Tauri platform icon set rather than hand-adjusting individual outputs.

U7 is a presentation and platform-adapter slice. It changes neither Git mutation semantics nor the typed editor-document boundary established by U6. Native directory access is limited to the user-selected path; the workbench does not receive a general filesystem browser capability. Platform icons remain Asterlyn-owned artwork and do not reuse Android Studio, JetBrains, or operating-system assets.

**Implemented and locally accepted on 2026-09-08.** Browser interaction checks found no activity-item pseudo-element and measured the active tile at the single `#3574f0` accent background. The Changes DOM contains no checkbox decoration; whole-row plain and modifier clicks retained `aria-selected` multi-selection, and focus remains visibly outlined. Commit details place the changed-file region before the collapsed semantic Commit information disclosure in DOM order, and the file list receives the flexible height. Hiding the Git tool removed both its panel and splitter while an open `repository.rs` Diff remained the active editor document; reopening Branches restored the same independent tool region.

A release-build native run used a fresh temporary application-data directory. It presented the Deepin system folder chooser titled `Open Git Repository`; cancelling left the empty workspace intact, and selecting `/home/gzq/asterlyn` then loaded 121 project paths, 53 visible commits, and the current branch through the existing repository-open flow. Browser mode instead showed the explicitly labeled simulation dialog. The desktop capability adds only `dialog:allow-open`; no filesystem plugin is initialized or exposed to the frontend. The original SVG now keeps a 40-unit transparent inset, and the pinned generator replaced the platform matrix. Automated checks cover 32–512 px desktop PNG outputs, Windows 16/24/32/48/64/256 frames, and macOS 128/256/512/1024 Retina resources.

Local acceptance passed 24 frontend, delivery, and icon tests, 21 Git-core tests, two Tauri tests, strict Clippy, TypeScript checking, the production frontend build, and Debian/RPM/AppImage packaging. Outputs were 12,981,720 bytes for the binary, 3,644,278 bytes for Debian, 3,646,538 bytes for RPM, and 91,347,448 bytes for AppImage. Relative to the exact M1.1 evidence, the binary and Debian package are cumulatively 10.69% and 10.58% larger respectively; that comparison spans U2–U7 and cannot attribute the increase to this slice alone.

The resource conclusion is **inconclusive** rather than a memory-improvement claim. Three short-settle runs against the Asterlyn checkout measured process-tree PSS at 213.07, 214.75, and 212.96 MiB (213.07 MiB median), summed RSS at 465.70, 467.37, and 465.67 MiB, and idle CPU at 0.00%, 0.33%, and 0.00%. Every PSS sample remained below the provisional 220 MiB ceiling, but the 15-second settle and different repository are not normalized with the earlier five-run, 60-second Rebased series. The interaction comparison is **improved** because all six requested redundancies or desktop mismatches are removed without changing Git truth or editor continuity. Windows/macOS chooser behavior and installed-icon appearance remain unverified, and Deepin emitted a non-fatal file-dialog heartbeat warning on application shutdown. The next product action remains Stage 3's bounded editor buffer and recovery contract; repeat the normalized memory series when that runtime surface lands.

### U8 — Diff and ref-navigation consistency

- Synchronize both axes of the two split Diff scrollers. The panes retain one shared source position while their vertical ranges remain normalized for unequal document heights.
- Scope the initial history snapshot to the current `HEAD`, then load the selected local branch, remote-tracking branch, or tag through a separate bounded exact-ref query.
- Reject branch-history responses unless their generation, repository root, and selected full ref still match the visible workbench.
- Keep the history filter available during loading and expose explicit loading, empty, failure, and retry states without replacing the selected branch inspector or active editor document.
- Make Local, Remote, and Tags semantic disclosure controls with visible counts, `aria-expanded`, native keyboard activation, and branch-row navigation restricted to expanded groups.

U8 corrects two U6 assumptions after real use. Branch selection now defines the commit-history scope instead of changing only the details column, and split Diff horizontal inspection is linked instead of independent. The Git boundary accepts only complete refs from `refs/heads/`, `refs/remotes/`, or `refs/tags/`, validates that the literal ref still exists, resolves it to a commit, and passes the resulting object ID to `git log` before a path separator. Tags that do not resolve to commits are rejected rather than interpreted as revision expressions. Repository snapshots use the object ID reported by porcelain status so their branch metadata and initial history describe the same observed `HEAD` even if another process moves a ref during refresh.

This slice does not add graph lanes, history pagination beyond the existing 150-commit bound, arbitrary revision expressions, branch mutation for remote refs, or persistence for disclosure state. It also does not claim complete-file Diff context; the existing bounded-patch limitation remains.

**Implemented and locally accepted on 2026-09-09.** Browser interaction against the deterministic Asterlyn demo showed two commits for the current feature branch and one for each main, remote main, and tag ref. Switching each row replaced the history before presenting the new result. A remote-history request deliberately overlapped a faster stage mutation; the late response was rejected and the workbench ended with the current feature branch selected, its two-commit `HEAD` history, and its branch inspector. Repeating the exercise after a tag history had settled produced the same current-`HEAD` reconciliation. Local, Remote, and Tags reported their expanded state, collapsed with a pointer, and toggled with both `Enter` and `Space` while their rows became hidden or visible without clearing the selected editor document.

The split Diff accepted input from either pane. Horizontal movement from the shorter side settled at `[44, 44]` pixels; movement from the wider side reached `[44, 164]` when the shorter side correctly hit its maximum, then reverse movement returned both to zero without feedback. Vertical movement settled at `[14, 14]`. A rapid diagonal sequence driven alternately from left, right, and left settled both panes at `{ left: 25, top: 14 }`. Two automated linked-scroll tests retain unequal-extent, delayed-event, alternating-input, disposal, and two-axis coverage; two ref-history reducer tests retain request-generation, repeated-ref, repository-change, and mutation invalidation coverage.

Final acceptance passed 28 frontend/delivery tests, 23 Git-core tests, one Tauri registry test, strict workspace Clippy, TypeScript checking, and the production frontend build. The single stage package run produced a 13,034,704-byte binary, 3,656,844-byte Debian package, 3,658,507-byte RPM, and 91,355,640-byte AppImage. Compared with U7's immediately preceding outputs, those are increases of 0.41%, 0.34%, 0.33%, and 0.01%; the packaging conclusion is **no material change**. Runtime memory was not remeasured, so U8 makes no memory-improvement claim.

The interaction conclusion is **improved**: all three reported inconsistencies are closed, stale or ambiguous ref results cannot overwrite visible truth, and Git mutations still return the workbench to fresh current-`HEAD` state. Remaining limits are the 150-commit bound, no graph lanes or pagination, absolute scroll alignment when one pane has no further horizontal content, non-persisted disclosure state, and no new Windows/macOS interaction evidence. The next product action is Stage 3's bounded editor buffer, save, and recovery contract.

### U9 — Compact Git information workspace

- Remove redundant Branches and Log title rows. Put a branch-or-tag filter directly above the ref tree and keep the commit search and current ref scope in the history toolbar.
- Present local refs, remote groups, remote refs, and tags with distinct original semantic icons. Never infer or display a favorite/default marker that Git did not report.
- Use one compact commit row for graph position, subject, truthful ref labels, author, absolute authored time, and abbreviated object identity. Columns may progressively hide when the middle pane becomes narrow, but subject and selection remain visible.
- Split selected-commit details into a changed-file browser above a compact commit summary. Their horizontal boundary is pointer- and keyboard-resizable, double-click resettable, viewport-clamped, and persisted only as a layout preference.
- Add an eye-labelled view control that switches changed files between a directory tree and one flat path-sorted list. Both projections preserve the same selected repository-relative path and open the same typed commit-Diff document.
- Show the selected commit's subject, abbreviated object identity, author name and email, absolute date, and parsed `HEAD`/local/remote/tag labels below the file browser. Raw decoration text remains presentation input rather than repository truth stored by the UI.

U9 changes only presentation and versioned workbench preferences. It adds no Git queries, branch favorites, inferred default branch, history pagination, path filtering, or mutation behavior. Android Studio supplies workflow-density evidence, while Asterlyn retains original markup, icons, styles, wording, and implementation.

**Implemented and locally accepted on 2026-09-09.** The browser demo presented each commit in a 28-pixel single row containing graph position, subject, up to two compact ref labels, author, absolute date, and abbreviated object ID. The previous Branches and Log title bands are gone: a live branch-or-tag filter now heads the left column, while the middle toolbar contains text/hash search, selected-history scope, and result count. Filtering `main` reduced the deterministic four-ref fixture to the matching local and remote rows, and `Escape` restored all four. Remote refs appeared beneath their real `origin` group. `HEAD`, local, remote, tag, and unclassified decorations use distinct original icons and colors; a local `feature/topic` is resolved against the snapshot rather than misclassified from its slash.

The right column measured 310 pixels high in the default browser viewport. Its initial 145-pixel commit-summary preference left 160 pixels for the changed-file browser plus the five-pixel separator. `ArrowUp` expanded the summary to 161 pixels and double-click restored 145; the same splitter supports pointer dragging, clamping, and versioned persistence through the shared workbench layout path. The eye-labelled control switched the two-file commit from a directory tree to a path-sorted flat list without changing the selected `crates/asterlyn-git/src/repository.rs` document, then restored the tree preference. The summary retained subject, object identity, author name and email, absolute date, first-parent comparison, and every named ref. Browser console inspection found no warning or error.

Final acceptance passed 32 frontend/delivery tests, 23 Git-core tests, two desktop tests, strict workspace Clippy, TypeScript checking, the production frontend build, and one Debian/RPM/AppImage package run. The release binary remained alive for the full 6,000-millisecond native smoke window. Outputs were 13,041,616 bytes for the binary, 3,663,940 bytes for Debian, 3,666,269 bytes for RPM, and 91,359,736 bytes for AppImage. Relative to U8 these increased by 0.05%, 0.19%, 0.21%, and less than 0.01%; the packaging conclusion is **no material change**.

The interaction conclusion is **improved**: the three columns now carry materially more visible information without adding repository work, tree/flat files remain one selection model, and the commit-summary boundary behaves like the other durable workbench splitters. Runtime memory was not remeasured, so U9 makes no memory-improvement claim. Remaining limits are the existing 150-commit bound, a single graph lane, no history path/user/date filters, non-persisted tree disclosure state, and no new Windows/macOS interaction evidence. Stage 3 editor foundations remain the next product slice.

### U10 — Topology-complete commit graph

- Remove the visible abbreviated object-ID column from history rows. Full and abbreviated IDs remain accepted by history filtering and remain available in commit details, where text selection is possible; the compact list does not spend permanent width on a non-interactive revision value.
- Make no ref selection the explicit `All refs` scope: the bounded history includes commits reachable from local branches, remote-tracking branches, and tags. Selecting a ref narrows the same column to that exact ref; activating the selected row again clears it and restores the snapshot's all-refs history.
- Request both all-refs and exact-ref history in topological order so every displayed child precedes its displayed parents, including both sides of a merge.
- Project the real ordered parent-object relationships into stable graph lanes. A merge commit fans out to every reported parent, non-current lanes continue through intervening rows, and lanes converge or retire only when the underlying parent graph requires it.
- Keep graph calculation in a pure presentation module and render original vector lines and nodes. Branch names, decorations, dates, and row adjacency must never invent ancestry.
- Give each graph row an accessible summary of its lane and parent count while preserving the existing compact row height, selection model, ref scoping, 150-commit bound, and stale-response rejection.

U10 is a history-read and presentation correction. Its one new history scope is the fixed local/remote/tag all-refs set; it adds no pagination, arbitrary revision expressions, mutation behavior, copied assets, or inferred relationship between unrelated refs. Repository refresh and successful mutations return to `All refs` so the ref list and graph cannot imply a narrower stale selection. A boundary lane may continue beyond the loaded 150-commit snapshot; that continuation is an honest indication of omitted history rather than a fabricated endpoint. When filtering hides intermediate commits, visible nodes retain only relationships backed by their own parent IDs and may therefore appear disconnected. User, date, path, and compound branch-filter controls remain a separate follow-up after the scope and topology contract are proven.

**Implemented and locally accepted on 2026-09-09.** The deterministic browser fixture opened with no selected ref, an `All refs` scope, and four reachable commits. Its two-parent merge fanned from lane one into two colored lanes, preserved the inactive lane while each parent was visited, and converged both parents at their shared root. Selecting local `main` replaced the middle list with its one-commit history; activating that selected row again removed its selected state and restored all four commits. The accessible graph summaries reported lane and parent counts, and extracted history-row text contained no abbreviated object IDs. Full and short IDs remain accepted by the text/hash filter and remain selectable in commit details.

The Git boundary now enumerates the displayed local, remote-tracking, and tag refs, deduplicates their observed object IDs together with a detached `HEAD`, and passes only those exact IDs to the shared topological history reader. Integration coverage proves that an all-ref snapshot contains divergent tips, ignores a non-commit tag as history, retains both parents of a merge, and places every loaded parent after its child. Exact-ref validation and stale-response rejection remain intact.

Final acceptance passed 35 frontend/delivery tests, 24 Git-core tests, two desktop tests, strict workspace Clippy, TypeScript checking, the production frontend build, and one Debian/RPM/AppImage package run. The release executable remained alive for the full 6,000-millisecond native smoke window. Outputs were 13,051,616 bytes for the binary, 3,669,498 bytes for Debian, 3,671,188 bytes for RPM, and 91,367,928 bytes for AppImage. Relative to U9 these increased by 0.08%, 0.15%, 0.13%, and 0.01%; the packaging conclusion is **no material change**.

The interaction conclusion is **improved**: the history graph now communicates real branch divergence and merge convergence, the default view includes every supported ref namespace, and selecting a ref is a reversible scope operation. Runtime memory was not remeasured, so U10 makes no memory-improvement claim. Remaining limits are the 150-commit bound, no history pagination, graph discontinuities when filtering hides intermediate commits, and no new Windows/macOS interaction evidence.

The post-U10 history-control order is based on truthfulness as well as implementation cost. Date presets, author values already present in the loaded snapshot, and case-sensitive text matching are low-cost presentation filters, but must be labelled as filtering the loaded 150 rows. Resolving `me`, first-parent/no-merge modes, and date-versus-topology order require typed Git query options and identity/config reads, so they form a second backend-aware slice. Path filtering and multi-ref selection require new query contracts; Recent/Favorites require explicit preference semantics; and linear-branch collapse requires graph-aware hidden-row composition. Those controls remain later work rather than incomplete look-alikes in U10.

### U11 — History filters and graph controls

U11 moves the deferred history controls ahead of Stage 3 and delivers them in two local implementation batches while retaining one stage-level package and push checkpoint.

#### U11A — Query and toolbar foundation

- Replace the passive history-scope label with compact `Branch`, `User`, `Date`, `Paths`, and graph-options controls beside text/hash search.
- Let Branch select any bounded set of exact displayed local, remote-tracking, or tag refs. An empty selection means `All refs`; direct activation in the left ref tree remains a one-ref shortcut, and activating that sole ref again returns to all refs.
- Resolve User choices by exact author email. `me` is resolved inside the Git boundary from the repository's effective `user.email`; no identity value is stored in application preferences.
- Apply 24-hour and seven-day Date presets, one exact repository-relative Path, first-parent/no-merge options, and topological/date ordering through a typed query. Git receives only validated refs, timestamps, author values, booleans, and a path after `--`; the UI rejects stale results by the complete normalized query identity.
- Keep text/hash matching within the returned 150-row window. Case-sensitive and regular-expression toggles affect the same fields as ordinary text search; an invalid expression presents an inline error and never runs or empties a Git query.

#### U11B — Durable ref shortcuts and linear collapse

- Store Favorites and a bounded most-recently-used ref list per repository. A favorite is always an explicit user choice; missing refs are not invented or sent to Git, and repository paths/ref names never enter layout persistence.
- Collapse only maximal loaded runs whose interior commits are undecorated, unselected, single-parent commits with one loaded child. Keep structural endpoints, replace at least two successive interior commits with one activatable dotted continuation row, and rewire only that presentation projection to the first retained ancestor.
- Expanding restores every original commit and parent edge without another Git query. Query, repository, selection, or result changes recompute collapse from immutable commits rather than mutating Git truth.

U11 deliberately does not add history pagination, arbitrary revision expressions, free-form pathspecs, persisted author/date/path filters, Git-log indexing, cherry-pick, or copied JetBrains assets. Menus use original Asterlyn markup and icons. The backend filters the complete walk before applying the existing 150-commit display bound; only text/hash matching is explicitly local to those loaded rows.

**Implemented and locally accepted on 2026-09-09.** The passive scope label is now a compact query toolbar. Branch accepts multiple exact local, remote-tracking, or tag refs and returns to the bounded all-refs snapshot when cleared. User combines exact loaded author emails with repository-effective `me`; Date supplies fixed 24-hour and seven-day lower bounds; Paths selects one tracked repository-relative file; and graph options switch topological/date order, first-parent traversal, and merge exclusion. Text/hash filtering remains local to the returned rows, now with independent case-sensitive and regular-expression controls. Invalid expressions retain the loaded result and present an inline diagnostic instead of issuing another Git request or showing an empty history.

Browser acceptance exercised the deterministic seven-commit, two-parent demo. Selecting local `main` returned one commit; adding `feature/graph-rendering` returned the two-ref union. Exact contributor filtering returned one commit, and path filtering for `crates/asterlyn-git/src/repository.rs` retained the matching non-root history. The final fixture contract proves that First Parent reduces the selected feature history from seven commits to six and No Merges reduces it to five. A three-commit undecorated linear run became one activatable dotted continuation while its retained child and ancestor stayed connected; activation restored all seven original rows without a Git request. Favorites and the eight-entry recent list survived reload under repository-specific preference keys, while missing refs are discarded before display or query construction.

At a 1,280 × 720 browser viewport, the full text-labelled toolbar, menus, graph, and three Git panes remained visible. At 1,024 × 720 the resizable middle pane reached 320 px; its five filter controls changed to labelled icon presentation with every button inside the toolbar and no horizontal overflow. Search, filter, and continuation controls expose accessible names and pressed/expanded state. Native buttons remain keyboard reachable and `Escape` closes the active menu; directional menu navigation is not yet implemented and remains an accessibility follow-up.

Final acceptance passed 45 frontend/delivery tests, 26 Git-core tests, two desktop tests, Rust formatting, strict workspace Clippy, TypeScript checking, the production frontend build, and one Debian/RPM/AppImage package run. The release executable remained alive for the full 6,000-millisecond native smoke window. Outputs were 13,195,928 bytes for the binary, 3,720,354 bytes for Debian, 3,722,556 bytes for RPM, and 91,417,080 bytes for AppImage. Relative to U10 those increased by 1.11%, 1.39%, 1.40%, and 0.05%. The package-size conclusion is **mixed**: the system binary and packages grew measurably, while the compatibility-heavy AppImage was materially unchanged. This is accepted for the typed query boundary and controls, with the binary trend retained as a later optimization signal.

Three short-settle Linux runs against the Asterlyn checkout measured process-tree PSS at 164.40, 164.98, and 165.33 MiB, summed RSS at 451.95, 452.32, and 452.77 MiB, and two-second idle CPU at 0.00%, 0.50%, and 0.00%. Every PSS sample remained below the provisional 220 MiB ceiling. The memory conclusion is **inconclusive**: the ten-second settle, short CPU window, repository state, and lack of a normalized U10 series do not support a reduction claim. The interaction conclusion is **improved** because all advertised controls now execute truthful typed queries or reversible presentation transforms rather than acting as decorative placeholders.

Remaining limits are the 150-commit result bound, no pagination or indexing, author choices sourced only from the initial loaded snapshot, two fixed date presets, one exact tracked path, repository-local unsynchronized ref shortcuts, and collapse limited to qualifying contiguous runs in the loaded projection. Windows/macOS installed interaction, directional menu navigation, arbitrary date ranges, free-form pathspecs, and linear-collapse behavior across unloaded history remain unaccepted. Stage 3 editor foundations are the next product slice.

### U11.1 — Root-qualified Branch and Paths correction

U11.1 corrects two interaction models that were functionally valid but structurally unlike the intended compact Git workflow. It also prevents a presentation-only submodule selector from routing commits to the wrong repository.

- Branch uses a short hierarchical launcher rather than presenting every ref in one scrolling list. `Select...` opens the independent root-qualified multi-ref editor; `Recent`, `Local`, `Tags`, and each configured remote open bounded submenus; explicit Favorites remain direct shortcuts. Both the launcher shortcut and left branch navigation represent one logical combination of ref kind and complete ref name, then expand it into every currently checked Git root where that ref exists; the editor alone owns additive and exact per-root selection plus favorite management. A multi-root logical row is history-only, while an empty ref selection continues to mean all refs in every active Git root.
- Paths uses `Select...`, `Select in Tree...`, in-session Recent values, and—when more than one initialized Git root exists—a Roots section. Each main repository or initialized submodule is represented by a checkbox and all are checked by default; users narrow history by unchecking modules, while at least one module remains selected. There is no separate `All roots` row or combined path/root clearing action. `Select...` accepts one or more exact known repository paths separated by new lines. `Select in Tree...` exposes tracked directories and files with multi-selection. These are literal repository-relative paths, not arbitrary Git pathspecs.
- Every root, ref, path, commit, detail request, and commit Diff carries a root-qualified identity. Root IDs are opaque presentation values issued by the Git boundary; the desktop bridge resolves them only through a freshly discovered catalog and never treats a client-supplied ID as a filesystem path.
- The root catalog contains the main repository plus recursively declared, initialized submodules whose canonical worktrees remain inside the main worktree. Missing, deinitialized, escaping, undeclared, or stale nested repositories are not selectable. Empty root selection means all catalogued roots; choosing Roots narrows the query without changing working-tree mutation scope.
- History is queried independently per effective root. Exact refs within one root form a reachability union, exact paths within one root form a path union, and ref/path dimensions intersect. Roots without a value for an active ref or path dimension contribute no broad fallback results. Results retain their per-root Git order and are deterministically interleaved by timestamp.
- History loads in 150-row pages. Reaching the bottom requests and appends the next page for the complete normalized Root/ref/user/date/path/graph query; reaching the top refreshes the currently loaded range so newly arrived commits can appear. Page identity, offset, root, and query generation participate in stale-response rejection, returned commits are deduplicated by root-qualified identity, and an explicit in-memory row ceiling prevents unbounded sessions. Selection is retained when its root-qualified commit remains present.
- Graph identities include both root and object ID. Parent lines never cross root boundaries, even if two repositories contain identical object IDs. Commit details and commit Diff are revalidated and opened against the selected commit's owning root. Submodule checkout, creation, stage, commit, fetch, pull, and push remain explicitly outside this correction.
- Existing unqualified Favorites and Recent refs migrate only to the main root. Path recents are session-local and root-qualified. Missing or relocated roots are discarded rather than rebound by name.

The UI builds directory trees and menu hierarchy from flat typed data; the Git core remains independent of DOM and Tauri. Graph projection retires parent lanes whose commits are absent from the loaded projection. When one exact ref per participating root is combined with author, path, merge-exclusion, or local text filtering, omitted direct parents are bridged to the next visible commit in that root so hidden commits cannot accumulate phantom lanes; multi-ref filtered topology remains conservative and does not infer ancestry. Cross-root reads and offset pages are not atomic, so a root failure fails the complete request instead of silently substituting main-repository data; a top refresh is the reconciliation path when repositories move between pages. Cross-root merge-base, backend-authored synthetic topology, durable pagination cursors, arbitrary revision expressions, arbitrary pathspecs, deinitialized-submodule browsing, and submodule mutations remain deferred.

**Implemented and locally accepted on 2026-09-09.** Branch now opens a compact launcher with `Select...`, Recent, Favorites, Local, per-remote, and Tags routes; only the independent editor performs additive selection and favorite changes. Paths now supplies exact multiline and tree selection, session Recent values, and per-module root checkboxes when the backend reports more than one initialized Git root. Every module is checked by default, users can narrow the query by unchecking modules, and the final selected module is retained so an empty UI selection cannot ambiguously mean both no roots and all roots. The main repository and recursively initialized contained submodules contribute independently queried, root-qualified refs, commits, paths, details, and Diff results. A fabricated or deinitialized root fails closed, and colliding object IDs cannot share a graph edge or UI selection key. All stage, commit, branch, fetch, pull, and push actions remain scoped to the main repository.

History now requests 150-row pages. Reaching the bottom appends older rows, while returning to the top after scrolling refreshes the currently loaded range. The request sequence, history generation, repository root, normalized query identity, and requested offset are all checked before a result is applied. Root-qualified duplicates are removed while the server offset still advances, the current selection survives reconciliation when its commit remains present, and a 3,000-row session ceiling bounds both the backend window and rendered history. Offset pages can move when refs change between requests; top reconciliation is the deliberate recovery path, not a claim of snapshot isolation.

Browser interaction exercised the hierarchical ref launcher, two-ref additive selection, multiline path selection through `Ctrl+Enter`, path-tree selection, and the inline rejection of an unknown path. At 1,024 × 700, all three Git panes and toolbar controls remained inside the workbench with no horizontal workbench overflow. This check also removed a stale pre-workbench media rule that had compressed the complete workbench to 280 px below 1,120 px. The browser console reported no warnings or errors. A real initialized-submodule fixture verified catalog discovery, ref/path intersection, commit details, commit Diff, project-file routing, fabricated-root rejection, and removal after deinitialization.

A follow-up comparison against the user's `superboost` fixture reproduced two root-qualified presentation defects. Both the main repository and initialized `vaLib` submodule contain `refs/heads/dev_dualspacepro`: the main repository's newest `xiabo` match is `9fa8130` from 2024-02-20, while the submodule-only result starts at `731ed01` from 2024-01-02. The compact selector now presents one logical branch choice, expands it to both exact root-qualified refs, and therefore restores the newer main-repository commit to the merged result. The previous graph projection also retained one unresolved lane for every parent hidden by the author filter. A 150-commit real `vaLib` sample now projects to one lane, and browser acceptance measured the compact filtered graph at 22 px with every displayed row in lane one. The result is **improved**: the latest commit agrees with the selected workspace-level branch scope and graph width no longer grows with filtered history depth. The bridge is intentionally limited to one exact ref per participating root; ambiguous multi-ref filtered history remains conservative until Git-authored simplified topology is introduced. TypeScript checking and all 60 frontend/delivery tests passed; packaging and remote publication remain deferred to the next stage boundary.

A subsequent audit found that the left branch navigation still exposed those root-qualified records as indistinguishable duplicate rows and dispatched only the clicked root. It now deduplicates Local, Remote, and Tags by logical identity within the active root set, reports root multiplicity on a shared row, and uses the same catalog-wide selection expansion as the toolbar shortcut. Browser acceptance injected one local ref in two Git roots: Local retained three logical rows rather than increasing to four, the shared row displayed `2 roots`, its scope title named two Git roots, and the two entry points returned identical commit rows. Activating the left row again restored all seven fixture commits. Multi-root rows intentionally open commit history rather than a branch mutation inspector. TypeScript checking and all 61 frontend/delivery tests passed; the result is **improved**, with packaging and remote publication still deferred to the next stage boundary.

Final acceptance passed 54 frontend/delivery tests, 28 Git-core tests, two desktop tests, Rust formatting, strict workspace Clippy, TypeScript checking, the production frontend build, the desktop bridge check, and one Debian/RPM/AppImage package run. The release executable remained alive for the full 6,000-millisecond native smoke window. Outputs were 13,406,544 bytes for the binary, 3,808,882 bytes for Debian, 3,811,100 bytes for RPM, and 91,507,192 bytes for AppImage. Relative to U11 those increased by 1.60%, 2.38%, 2.38%, and 0.10%. The package-size conclusion is **mixed**: the compatibility-heavy AppImage remains materially stable, while the binary and system packages grew measurably and should remain visible as an optimization signal.

Three short-settle Linux runs against the Asterlyn checkout measured process-tree PSS at 224.39, 223.25, and 226.19 MiB, summed RSS at 456.44, 455.35, and 458.79 MiB, and two-second idle CPU at 1.00%, 0.00%, and 1.50%. All PSS samples exceeded the provisional 220 MiB ceiling, by 3.25–6.19 MiB, and the 224.39 MiB median is materially above U11's 164.98 MiB short-settle median. The observed resource conclusion is **regressed**. Attribution remains inconclusive because both evidence sets use a short settle rather than the standard five-run, 60-second baseline and no per-process comparison was retained. The next performance action is a normalized 60-second series with per-process attribution and matched repository state before deciding whether multi-root state, WebKit variability, or another runtime factor requires remediation; this stage makes no memory-improvement claim.

The interaction conclusion is **improved**: the reported Branch and Paths interaction mismatches are corrected, logical branch shortcuts include matching refs from every selected module, filtered history cannot accumulate phantom graph lanes, initialized submodules cannot alias main-repository data, and long histories no longer stop silently at 150 commits. Remaining limits are the 3,000-row session ceiling, offset rather than durable-cursor pagination, UTF-8-only submodule paths, a bounded tracked-file catalog, session-only path recents, conservative multi-ref filtered topology, no submodule mutations, no backend-authored cross-root synthetic topology, and no Windows/macOS installed interaction evidence. Stage 3 editor foundations remain next, with the normalized memory comparison retained as an explicit acceptance follow-up.

### E1 — Safe multi-tab text editing

E1 replaces the read-only project-file placeholder with a bounded explicit-save editor loop while preserving the permanent workbench contract. The center now retains up to 20 deduplicated editable text tabs and one replaceable Diff preview. A new file beyond that state bound retires the oldest inactive clean tab; dirty and saving buffers are protected, so navigation is refused only when every retained buffer requires user action. Text tabs survive repository refresh, and repository/window transitions must Save All or Cancel when any buffer is dirty. Native reads and saves repeat exact Git-root-qualified authorization, and save uses optimistic revision checking plus same-directory atomic replacement; conflicts retain the local buffer and offer no force overwrite.

Local acceptance on 2026-09-09 records six workspace tests, 28 Git tests, four desktop-library tests, 68 frontend script tests, passing TypeScript and production builds, a complete deterministic browser interaction journey, and a six-second native Linux liveness smoke. The real desktop-library repository test proves tracked-file read/save, external-change conflict preservation, ignored-path denial, and fresh authorization revocation. An independent security review confirmed that the native dirty-close callback synchronously prevents the close event before starting its asynchronous Save-or-Cancel flow.

The comparison conclusion is **improved** for interaction and correctness. The build produced 50.40 kB CSS (10.00 kB gzip), 509.34 kB JavaScript (150.19 kB gzip), and a 2,048.12 kB JavaScript source map, but no normalized pre-E1 asset or 60-second resource run is available; bundle movement and memory therefore remain **inconclusive**. Packaging and remote publication are intentionally deferred to the agreed larger Stage 3 checkpoint. Remaining limits include the two-MiB UTF-8 boundary, 20-tab cap, explicit save only, no syntax modes/watcher/recovery/discard/file mutations, adapter-local undo lifetime, the visible large-chunk warning, incomplete ACL/xattr preservation, the documented final path-validation race, non-Unix hard-link parity, and no installed Windows/macOS interaction evidence. E2 bounded navigation and search is next.

### E2.1 — Keyboard navigation and bounded search

E2.1 adds one transient keyboard-first surface for project files, repository-scoped recents, workspace text, and commands. Search candidates are created only by a fresh Git catalog inside the desktop boundary, then passed to a pure sequential scanner with explicit file, total-byte, match, preview, skip-reporting, and cancellation bounds. Results carry exact Git-root-qualified identities, E1 revisions, and normalized UTF-16 positions. Opening a result repeats E1 authorization and rejects dirty, saving, stale, wrong-root, or invalid-range navigation. Active-file CodeMirror replacement remains undoable and explicit-save-only; there is no workspace replacement action.

Local acceptance passed 13 workspace, 28 Git, seven desktop, and 83 frontend tests; formatting, strict Clippy, TypeScript checking, production build, a complete browser keyboard/accessibility journey, and six-second native liveness. The named 161-candidate checkout produced a 15.908-millisecond median combined catalog-and-scan path over ten iterations. Interaction, correctness, and fixture latency are **improved**. The frontend JavaScript bundle grew to 533.11 kB raw and 156.05 kB gzip, so bundle size is **regressed**. Three short-settle release runs measured 232.11–232.49 MiB PSS with a 232.41 MiB median, 8.02 MiB above U11.1 and above the provisional ceiling. Observed idle resources are **regressed**, but attribution is **inconclusive** because E1 lacks a matched series and the standard 60-second protocol was not run.

Detailed methods, absolute results, limitations, and next actions are recorded in [`E2.1 evidence`](../benchmarks/2026-09-09-e2-1-navigation-search.md). Packaging and remote publication remain deferred to the larger Stage 3 checkpoint. E2.2a on-demand syntax highlighting is the first usability follow-up, then E2.2b resumes read-only search refinement; multi-file replacement waits for E2.3 recovery guarantees, and the normalized resource comparison remains open before the stage checkpoint.

### E2.2a on-demand syntax highlighting

E2.2a is locally accepted. Filename-based selection covers the current 143-entry CodeMirror language catalog, while the catalog and selected parser load only after a text editor mounts. A generation gate discards late results from replaced tabs, parser failure preserves plain-text editing, and the Asterlyn token palette has an automated 4.5:1 normal-text contrast floor. Production-browser interaction confirmed TypeScript and Markdown styling, edit/save continuity, and clean language switching.

Detailed functional, accessibility, build, lazy-load, and focused heap evidence is recorded in [`E2.2a evidence`](../benchmarks/2026-09-09-e2-2a-syntax-highlighting.md). Readability is **improved**; main and packaged frontend footprints are **regressed**; and the one-run browser heap impact is **inconclusive**. Packaging and remote publication remain deferred. E2.2b search refinement is next, while catalog packaging and normalized native resources remain explicit Stage 3 checkpoint work.

### E2.2b bounded search refinement

E2.2b is locally accepted. The existing Find in Files surface now offers case-sensitive literal or line-local regular-expression matching, complete workspace-path include/exclude globs, and zero to three context lines. All controls share one immutable request identity; changing any value cancels and invalidates old results. The Rust host retains fresh Git authorization, the established file/byte/match limits, typed partial coverage, exact UTF-16 navigation ranges, and no index or background work.

Detailed functional, accessibility, latency, bundle, and focused heap evidence is recorded in [`E2.2b evidence`](../benchmarks/2026-09-09-e2-2b-search-refinement.md). Interaction and correctness are **improved**, default fixture latency is **no material change**, frontend size is **regressed**, and the one-run search-surface heap movement is **inconclusive**. No package or remote push is made for this sub-slice. E2.3 recoverable workspace replacement is next; the measured 15.629-millisecond default core path does not justify a persistent index.

### E2.3 recoverable workspace replacement

E2.3 is locally accepted. Workspace replacement is a reviewed transaction rather than a sequence of hidden saves: it repeats fresh Git authorization, rejects structurally incomplete coverage, previews every changed file with explicit file selection, preflights all revisions, journals exact original/proposed bytes before mutation, and keeps recovery across restart until verified keep or conservative rollback. Cancellation and partial failure restore files that still contain the proposal; independent external changes survive and remain visible as unresolved recovery. Dirty open targets block apply or rollback, while completed mutations refresh clean tabs, search results, and working-tree state without discarding the editor or history filters.

Detailed safety, fault, interaction, accessibility, latency, build, and focused heap evidence is recorded in [`E2.3 evidence`](../benchmarks/2026-09-10-e2-3-recoverable-replacement.md). Interaction and recovery correctness are **improved**, named-fixture planning is interactive, frontend size is **regressed**, and focused memory remains **inconclusive**. No package or remote push is made for this sub-slice. E2 is complete and E3 editor groups and preferences is next.

### E3.1 workbench navigation and preference foundation

E3.1 is locally accepted. The project tree now treats files and folders as one selectable navigation model, supplies compact locate-current-file and recursive expand/collapse actions, preserves selected folders while disclosure changes, and uses current Git status for semantic path colors. A separately bounded ignored-path display catalog lets the tree explain hidden build/configuration output without making those paths readable or writable through the workspace boundary. Working and commit Diff documents reuse the same on-demand CodeMirror language loader and token theme as editable files in both unified and split layouts.

Settings is a real full-workspace route with General, Appearance, Editor, Version Control, and Languages groups. Application and editor typography, editor line height, tab display width, Diff layout, and whitespace visibility are validated, persisted, and applied immediately. Localization, light/system themes, and per-language formatting are visible only as planned capability boundaries rather than inert controls.

Browser acceptance exercised file and directory selection, recursive collapse/expand with retained selection, locating `src/app.ts` from a working-tree Diff, settings navigation and reload persistence, live typography changes, and parser-ready TypeScript tokens in one unified and both split Diff editors. The project-tree state exposed `aria-selected`/`aria-expanded`; icon actions and settings navigation had explicit names; colors remained supplementary to status glyphs and labels. The populated deterministic workbench contained 830 DOM nodes, two CodeMirror instances, and 18 visible project rows; this is a scope observation, not a memory measurement.

Detailed functional, security-boundary, accessibility, build, and native-liveness evidence is recorded in [`E3.1 evidence`](../benchmarks/2026-09-10-e3-1-workbench-preferences.md). Interaction is **improved**, frontend size is **regressed**, native executable size has **no material change**, and memory impact remains **inconclusive**. No package or remote push is made for this sub-slice. E3.2 editor groups and tab movement is next.

### E3.1 desktop-shell correction

The shell follow-up removes the duplicate brand block, moves the active-project identity to the title bar's leading edge, adopts the locally installed Android Studio/JetBrains 13 px typography baseline, raises the title/menu bar to 48 px, and enlarges its primary actions. A vertical wheel over an overflowing editor-tab strip now moves it horizontally. Opening a first project remains an in-place action; selecting a different project from an occupied window prompts for current-window replacement or a new independently authorized native window.

The project-identity follow-up reduces that leading control to the project name and one disclosure arrow. Its absolute path appears only as hover tooltip metadata, while the menu offers Open and a bounded recent-project list containing names and paths without duplicating a separate `Open Projects` section. Files and Changes share a one-title dock header with the current count immediately after the title. The left dock and bottom Git dock each keep a visible top-right Hide action; this is the required pattern for every future independently hideable tool window. Ignored directory rows now inherit the same semantic ignored-path color as ignored files, and the original recursive expand/collapse marks use separated directional chevrons that remain legible at compact toolbar size.

Platform launch assets now come from one full-canvas SVG and retain only the desktop files actually declared by the bundle. Detailed standards, interaction, isolation, size, liveness, and local Debian package evidence is recorded in [`E3.1 desktop-shell correction evidence`](../benchmarks/2026-09-10-e3-1-desktop-shell.md). Interaction and asset consistency are **improved**, frontend size has **no material change**, native executable size is **regressed**, and memory remains **inconclusive**. This correction does not advance the milestone or trigger a remote push; E3.2 remains next.

Manual acceptance then found that the custom close button and `Alt+F4` both stalled at Tauri's close-request permission boundary. The focused correction grants project windows the destroy operation used by `onCloseRequested`, directly destroys only after the dirty-buffer gate approves closure, and passes real pointer and system-close runs. The refreshed Debian artifact and exact evidence remain on the E3.1 desktop-shell page; E3.2 sequencing is unchanged.

The approved platform-chrome follow-up now creates every project window through one Rust factory. macOS keeps native decorations and left-side traffic lights under an overlay title bar, with frontend spacing reserved for those controls and duplicate custom buttons suppressed. Windows and Linux keep the established undecorated title bar and right-side custom controls. Linux release-window maximize and close paths pass without changing dirty-buffer or multi-window ownership semantics; drag/minimize automation and installed macOS visual interaction remain manual checks. The detailed record and refreshed Debian artifact remain on the same E3.1 evidence page, and E3.2 sequencing is unchanged.

Subsequent installed-platform review corrected the remaining launcher asymmetry without changing editor or Git behavior. Linux now advertises the declared 128-pixel native window icon instead of Tauri selecting the former first 32-pixel PNG, while macOS receives a generated ICNS-only 82% centered visual envelope so the full-canvas Windows/Linux mark does not appear oversized in Launchpad. A separate protected, manual macOS distribution workflow now encodes Developer ID, app-and-DMG notarization, installed-form Gatekeeper verification, and two-architecture checksum gates; the ordinary preview workflow remains ad-hoc and secret-free. The workflow is ready but not accepted as a Gatekeeper fix until Apple credentials, protected `main`, the `macos-signing` environment, a successful run, and clean-machine installation evidence exist. Exact diagnosis, tests, hashes, and package evidence are recorded in the E3.1 desktop-shell page; E3.2 sequencing remains unchanged.

The editor typography follow-up replaces the platform-dependent fallback chain with the pinned, OFL-licensed upstream JetBrains Mono variable font as the only bundled editor family. Code and Diff surfaces, editor gutters, and rendered Markdown code share that Latin glyph source, 400 weight, disabled discretionary/contextual ligatures, and the same preference variables. A CodeMirror-calibrated 14-pixel size and 1.35 line height remain the baseline because identical numbers do not produce identical JBR and WebView geometry. Normal zero letter spacing remains the default; users can still choose bounded alternatives.

Cascadia Code, Fira Code, Source Code Pro, and IBM Plex Mono are selectable but not packaged. First selection performs one bounded fetch from a versioned allowlist, validates SHA-256 before use, caches only verified bytes, and persists only the catalog identifier. Offline, integrity, and decoding failures leave the previous family active and expose a retry action. A profile-cache failure is reported but permits the already verified face for the current window. This remains a presentation adapter: it adds no filesystem permission, native network command, service, parser, watcher, index, or recurring task.

### Git interaction rendering correction

The Git follow-up replaces commit-selection-wide workbench rendering with region-scoped updates. Selecting a commit now retains the branch and history DOM, scroll position, filter controls, and graph projection; it changes row selection immediately and refreshes only the third column after the root-qualified file query. Commit-file selection similarly updates only the relevant rows and editor Diff. A bounded session cache makes repeated commit inspection local, while stale async results remain rejected by repository, root, object, selection, and generation identity.

Splitter pointer gestures reuse their initial validated range rather than forcing a layout read after each CSS width write. Git panes and history rows isolate layout/paint work, supported webviews defer offscreen row rendering, and pointer hit testing is suspended inside moving columns for the drag lifetime. The detailed diagnosis, absolute interaction checks, build movement, limitations, and next action are recorded in [`Git interaction rendering evidence`](../benchmarks/2026-09-11-git-interaction-rendering.md). Interaction is **improved**, sampled Git detail latency and package size show **no material change**, and memory remains **inconclusive**. This is a focused correction and does not advance the Stage 3 sequence.

### Checked commit workflow correction

The Changes tool now follows the compact commit workflow observed in Android Studio while retaining Asterlyn-owned markup, icons, wording, and Git policy. One toolbar contains Refresh, tracked-file Revert, complete local Diff, tree/flat view, recursive expand, and recursive collapse. Versioned Changes and Unversioned Files each appear once beneath group checkboxes; directory checkboxes are tri-state, all newly discovered paths are included by default, and the checked set survives tree/flat projection and ordinary refresh while an existing path remains changed. Row selection is a separate single-file state: clicking a file opens its `HEAD`-to-worktree Diff and never toggles commit inclusion. The commit composer occupies a persisted, resizable lower region and commits exactly the checked whole-file set without a manual stage step.

The desktop boundary serializes every local and remote write per canonical repository root. Before commit, the Git core repeats status and exact `FileChange` identity checks, rejects conflicts and submodule paths, uses literal pathspecs, and creates a path-limited commit from current worktree content while preserving unrelated index entries. Temporary intent-to-add entries make selected untracked files eligible without absorbing other staged files and are removed after an ordinary unchanged-`HEAD` hook or command rejection. Parent and changed-path verification guards the published object ID. External Git remains outside the process lock: if it advances `HEAD`, Asterlyn preserves that history instead of rolling back to stale state, refreshes, and either reports command failure or a successful-but-unverified do-not-retry outcome. Revert is intentionally limited to a confirmed selected tracked path and refuses untracked, newly added/copied, conflicted, submodule, unborn, or dirty-editor cases; no `git clean` or broad reset is exposed.

Deterministic browser acceptance verified five default-checked paths, checkbox-only inclusion changing `Commit 5` to `Commit 4` without replacing the editor, single-click `src/app.ts` opening a complete local Diff, tree/flat switching without losing the excluded path, ten directory disclosures collapsing to zero and expanding back to ten, and pointer resizing increasing the commit composer from 230 to 263 pixels. The detailed test, build, package, limitations, and conclusion are recorded in [`checked commit workflow evidence`](../benchmarks/2026-09-11-checked-commit-workflow.md). Interaction and commit-boundary correctness are **improved**. Amend, commit-and-push composition, multi-row destructive selection, untracked deletion, conflict resolution, and submodule commit routing remain explicit later slices.

## Sequencing rule

U1–U3 are published as the first usability phase, U4 is published as the safe local-branch slice, U5 is locally accepted as the remote daily-loop slice, U6 is locally accepted as the persistent workbench contract, U7 closes the highest-friction desktop interaction mismatches, U8 closes Diff/ref navigation consistency, U9 closes compact Git information layout, and U10 closes all-ref topology presentation. U11 history filters and graph controls precede Stage 3; E1 is locally accepted as the safe editor foundation, E2.1–E2.3 complete bounded navigation, on-demand syntax highlighting, refined search, and recoverable workspace replacement, and E3.1 establishes project navigation plus honest implemented preferences. Its desktop-shell, Git-rendering, and checked-commit corrections fix scale, icon generation, tab overflow, multi-project window ownership, splitter layout forcing, commit-selection-wide rerenders, and the obsolete manual-stage commit interaction without changing the sequence. E3.2 editor groups and tab movement is next. Windows/macOS interactive release checks remain open; platform-specific polish becomes blocking again before an artifact is described as a release candidate, not before useful feature development.
